editor:
  config:
    htmlSupport:
      allow:
        - name: placeholder
          classes: ['placeholder']

    importModules:
      - { module: '@resterland/rlt3sitepackage/placeholder.js', exports: [ 'Placeholder' ] }

    extraPlugins:
      - Placeholder

processing:
  allowTags:
    - placeholder
