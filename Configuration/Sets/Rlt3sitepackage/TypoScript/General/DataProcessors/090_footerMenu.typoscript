page.10.dataProcessing {
    # available as {footerMenu} in Fluid template
    90 = menu
    90 {
        as = footerMenu
        special = directory
        special.value = {$footer.navigation.menuRoot}
        includeNotInMenu = {$footer.navigation.includeNotInMenu}
    }
}
