# Language menu processors for Header and Footer
page.10.dataProcessing {
    # available as {languagemenu} in Fluid template
    40 = language-menu
    40 {
        as = languagemenu
        languages = {$header.languagenavigation.languageValue}
    }
    45 = language-menu
    45 {
        as = languagemenu
        languages = {$footer.languagenavigation.languageValue}
    }
}
